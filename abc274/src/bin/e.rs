use proconio::*;

fn main() {
    input! {
        n: usize, m: u32,
        ps: [(f64, f64); n + m],
    }
    let mut dist = [[1000000000000000000f64; n + m]; n + m];
    for i in 0..n
    let mut dp = vec![vec![(1i64) << 63; n]; 1 << n];
    dp[(1 << n) - 1][0] = 0;
    for 
}
